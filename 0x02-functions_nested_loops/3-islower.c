#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
 * islower - checkd for lowercase characters
 * Return: return 1 for lowercase ch and return 0 otherwise
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
