#include <stdio.h>
#include <stdlib.h>
#include <string.h>
/**
 * Description:main entry point
 *
 * Return: return zero (success)
 */

int main(void)
{
	int i;
	for (i = 0; i <= 9; i++)
		printf("%d", i);
	putchar('\n');
	return (0);
}
