#include <stdio.h>
#include <stdlib.h>

/**
 * Description:main - The main entry point
 *some other codes goes in here
 *
 * Return: Always return 0 (success)
 */

int main(void)
{
	char x[100] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", x);
	return (0);
}
