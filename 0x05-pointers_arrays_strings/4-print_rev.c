#include "main.h"
/**
 * print_rev - this funcion prints sets of characters(strings) in reverse order
 * @s: the pointer
 * Return: nothing
 */
void print_rev(char *s)
{
	int i;
	for (i = 0; i < 1; i++)
	{
		while (s[i] != '\0')
			

