#include "main.h"
/**
 * _puts - this function prints a string followed by a new line to stdout
 * @str: pointer
 * Return: nothing
 */
void _puts(char *str)
{
	for (; *str != '\0'; str++)
	{
		_putchar(*char);
	}
	_putchar('\n');
}
