#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
 * _isdigit -> checks for digits
 * @c: character argument
 * Return: 1 if digit and 0 otherwise
 */
int _isdigit(int c)
	return( c >= 0 && c <= 9);
